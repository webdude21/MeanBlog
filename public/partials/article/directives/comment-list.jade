section.comments
    h4 Comments:
    .row
        .col-lg-12.pull-left
            blockquote(ng-repeat="comment in comments")
                p {{comment.body}}
                small.pull-right comment by: {{comment.author.username}} on {{comment.date | date:'d MMMM y, HH:mm:ss'}}
        ul.pager
            li.previous(ng-hide="comemntsOnThisPage < 10")
                a(ng-click="older()" href="") ← Older
            li.next(ng-hide="commentsPage <= 1")
                a(ng-click="newer()" href="") Newer →